# ===================================
# REVERB REAL-TIME CONFIGURATION
# ===================================

# Broadcasting Driver (set to reverb for real-time features)
BROADCAST_CONNECTION=reverb

# Reverb Server Configuration
REVERB_APP_ID=local
REVERB_APP_KEY=your-reverb-app-key
REVERB_APP_SECRET=your-reverb-app-secret
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# Reverb Server Settings
REVERB_SERVER_HOST=0.0.0.0
REVERB_SERVER_PORT=8080
REVERB_SERVER_PATH=""

# Vite Configuration for Reverb
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# ===================================
# USAGE INSTRUCTIONS
# ===================================
# 1. Copy these variables to your .env file
# 2. Replace 'your-reverb-app-key' and 'your-reverb-app-secret' with actual values
# 3. Run: php artisan reverb:start --host=0.0.0.0 --port=8080
# 4. For production, use SSL and proper domain configuration
# ===================================
